[build]
builder = "nixpacks"

[deploy]
startCommand = "node scripts/migrate-to-pg.js && npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
